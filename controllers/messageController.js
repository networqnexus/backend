const Message=require("../models/Message"),User=require("../models/User");
exports.getConversations=async(req,res)=>{try{const me=await User.findById(req.user.id).populate("connections","name username avatarUrl headline lastSeen hideOnlineStatus");const conversations=await Promise.all(me.connections.map(async conn=>{const lastMsg=await Message.findOne({$or:[{sender:req.user.id,receiver:conn._id},{sender:conn._id,receiver:req.user.id}]}).sort({createdAt:-1});const unread=await Message.countDocuments({sender:conn._id,receiver:req.user.id,read:false});return{user:conn,lastMessage:lastMsg,unread};}));conversations.sort((a,b)=>new Date(b.lastMessage?.createdAt||0)-new Date(a.lastMessage?.createdAt||0));res.json({success:true,conversations});}catch(e){res.status(500).json({success:false,message:"Server Error"});}};
exports.getMessages=async(req,res)=>{try{const{userId}=req.params;const messages=await Message.find({$or:[{sender:req.user.id,receiver:userId},{sender:userId,receiver:req.user.id}]}).sort({createdAt:1}).populate("sender","name username avatarUrl");await Message.updateMany({sender:userId,receiver:req.user.id,read:false},{read:true});res.json({success:true,messages});}catch(e){res.status(500).json({success:false,message:"Server Error"});}};
exports.sendMessage=async(req,res)=>{try{const{text}=req.body,{userId}=req.params;if(!text?.trim())return res.status(400).json({success:false,message:"Message required"});const message=await Message.create({sender:req.user.id,receiver:userId,text});await message.populate("sender","name username avatarUrl");res.status(201).json({success:true,message});}catch(e){res.status(500).json({success:false,message:"Server Error"});}};
